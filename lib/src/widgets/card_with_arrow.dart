import 'package:flutter/material.dart';

class CardWithArrow extends StatelessWidget {
  const CardWithArrow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card();
  }
}