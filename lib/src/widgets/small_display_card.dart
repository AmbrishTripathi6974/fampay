import 'package:flutter/material.dart';

class SmallDisplayCard extends StatelessWidget {
  const SmallDisplayCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}